---
phase: 01-foundation-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - game/project.godot
  - game/core/game_events.gd
  - game/core/main.tscn
  - game/core/main.gd
autonomous: true
requirements: [FOUND-01, FOUND-04, FOUND-05, FOUND-06]

must_haves:
  truths:
    - "Godot project opens in editor with Compatibility renderer shown in title bar"
    - "GameEvents autoload is accessible from any script via GameEvents.signal_name"
    - "Keyboard input (WASD + arrows) registers as named actions in InputMap"
    - "Physics interpolation is enabled at the engine level"
  artifacts:
    - path: "game/project.godot"
      provides: "Engine configuration: renderer, input actions, physics, autoloads"
      contains: "gl_compatibility"
    - path: "game/core/game_events.gd"
      provides: "Signal bus with full scaffold for all 14 game systems"
      min_lines: 30
    - path: "game/core/main.tscn"
      provides: "Main scene that Godot loads on startup"
    - path: "game/core/main.gd"
      provides: "Pipeline verification script testing autoloads and input"
  key_links:
    - from: "game/project.godot"
      to: "game/core/game_events.gd"
      via: "autoload declaration"
      pattern: 'GameEvents="\*res://core/game_events.gd"'
    - from: "game/project.godot"
      to: "game/core/main.tscn"
      via: "main scene path"
      pattern: 'run/main_scene="res://core/main.tscn"'
---

<objective>
Create the Godot 4.6 production project with correct engine configuration, input system, GameEvents signal bus, and physics interpolation.

Purpose: This is the foundation every other phase builds on. Getting renderer, physics, input, and signal bus right from the first commit prevents cascading issues in all 8 downstream phases.
Output: A Godot project at `game/` that opens in editor, has Compatibility renderer, 8 input actions with dual key bindings, a full GameEvents signal scaffold, and physics interpolation enabled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-pipeline/01-RESEARCH.md
@docs/engine-reference/godot/VERSION.md
@docs/engine-reference/godot/breaking-changes.md
@docs/engine-reference/godot/current-best-practices.md
@prototypes/combat-racing-core/project.godot
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Godot project with correct engine configuration and input actions</name>
  <files>game/project.godot, game/.godot/ (engine-generated)</files>
  <read_first>
    - prototypes/combat-racing-core/project.godot (reference for format -- do NOT copy, it has Forward Plus which is WRONG)
    - docs/engine-reference/godot/breaking-changes.md (4.4-4.6 config changes)
    - docs/engine-reference/godot/current-best-practices.md (4.6 patterns)
    - .planning/phases/01-foundation-pipeline/01-RESEARCH.md (Pattern 1: project.godot Configuration, Pattern 3: Input Action Definition)
  </read_first>
  <action>
Create the `game/` directory structure and write `game/project.godot` from scratch (do NOT copy from prototype).

Directory structure to create:
```
game/
  core/          (autoloads -- scripts go here in next task)
  track/         (empty -- used in Plan 02)
  tests/         (empty -- used in Plan 02)
  export/        (empty -- used in Plan 02 for WebGL export)
```

Write `game/project.godot` with these exact settings (per D-01, D-02, FOUND-01, FOUND-04, FOUND-06):

```ini
config_version=5

[application]
config/name="Road Rage Rodeo"
run/main_scene="res://core/main.tscn"
config/features=PackedStringArray("4.6", "GL Compatibility")

[autoload]
GameEvents="*res://core/game_events.gd"

[display]
window/size/viewport_width=1280
window/size/viewport_height=720
window/stretch/mode="canvas_items"

[input]
move_forward={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":87,"key_label":0,"unicode":119,"location":0,"echo":false,"script":null)
, Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194320,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
]
}
move_back={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":83,"key_label":0,"unicode":115,"location":0,"echo":false,"script":null)
, Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194322,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
]
}
steer_left={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":65,"key_label":0,"unicode":97,"location":0,"echo":false,"script":null)
, Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194319,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
]
}
steer_right={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":68,"key_label":0,"unicode":100,"location":0,"echo":false,"script":null)
, Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194321,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
]
}
attack={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":32,"key_label":0,"unicode":32,"location":0,"echo":false,"script":null)
]
}
nitro={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194325,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
]
}
restart={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":82,"key_label":0,"unicode":114,"location":0,"echo":false,"script":null)
]
}
pause={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194305,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
]
}

[physics]
common/physics_ticks_per_second=60
common/physics_interpolation=true
common/max_physics_steps_per_frame=2

[rendering]
renderer/rendering_method="gl_compatibility"
anti_aliasing/quality/screen_space_aa=2
```

Key codes used (Godot physical_keycode values):
- W=87, S=83, A=65, D=68
- Up=4194320, Down=4194322, Left=4194319, Right=4194321
- Space=32, Left Shift=4194325, R=82, Escape=4194305

IMPORTANT: The `config/features` line MUST say `"GL Compatibility"` NOT `"Forward Plus"`. The prototype has Forward Plus -- that is WRONG for WebGL.

After writing project.godot, open the project once with Godot CLI to generate the `.godot/` directory:
```bash
/Users/haocheng_mini/Downloads/Godot.app/Contents/MacOS/Godot --path game/ --headless --quit 2>/dev/null || true
```

NOTE: The exact InputEventKey serialization format may need adjustment. An alternative approach is to write a minimal project.godot WITHOUT the [input] section, open it in Godot editor headlessly, and use a GDScript tool script to configure input actions programmatically. If the raw serialization format causes parse errors, fall back to:
1. Write project.godot with only [application], [autoload], [display], [physics], [rendering] sections
2. Create a setup script that uses `ProjectSettings.set_setting()` and `ProjectSettings.save()` to configure input
3. Run that script once via `godot --path game/ --headless -s setup_input.gd`

Per D-01, the folder structure follows Godot-idiomatic feature folders with autoloads in `game/core/`.
  </action>
  <verify>
    <automated>grep -q 'gl_compatibility' /Users/haocheng_mini/Documents/projects/my-game/game/project.godot && grep -q 'physics_interpolation=true' /Users/haocheng_mini/Documents/projects/my-game/game/project.godot && grep -q 'GameEvents' /Users/haocheng_mini/Documents/projects/my-game/game/project.godot && grep -q 'move_forward' /Users/haocheng_mini/Documents/projects/my-game/game/project.godot && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - game/project.godot contains `renderer/rendering_method="gl_compatibility"` (FOUND-01)
    - game/project.godot contains `config/features=PackedStringArray("4.6", "GL Compatibility")` -- NOT "Forward Plus"
    - game/project.godot contains `physics_interpolation=true` (FOUND-06)
    - game/project.godot contains `physics_ticks_per_second=60` (FOUND-06)
    - game/project.godot contains `GameEvents="*res://core/game_events.gd"` under [autoload]
    - game/project.godot contains all 8 input actions: move_forward, move_back, steer_left, steer_right, attack, nitro, restart, pause (FOUND-04)
    - game/project.godot contains `screen_space_aa=2` (SMAA)
    - game/project.godot contains `run/main_scene="res://core/main.tscn"`
    - Directories exist: game/core/, game/track/, game/tests/, game/export/
  </acceptance_criteria>
  <done>Godot project.godot exists with Compatibility renderer, physics interpolation, 8 dual-mapped input actions, SMAA, and GameEvents autoload declaration</done>
</task>

<task type="auto">
  <name>Task 2: Create GameEvents signal bus and main verification scene</name>
  <files>game/core/game_events.gd, game/core/main.gd, game/core/main.tscn</files>
  <read_first>
    - game/project.godot (just created -- verify autoload path matches)
    - .planning/phases/01-foundation-pipeline/01-RESEARCH.md (Pattern 2: GameEvents Signal Bus, Code Examples section)
    - design/gdd/systems-index.md (14 systems that need signal coverage)
  </read_first>
  <action>
Create three files per D-03, D-04, and FOUND-05:

**1. `game/core/game_events.gd`** -- Full signal bus scaffold (per D-04: scaffold ALL signals on day one):

```gdscript
extends Node
## Global event bus for cross-system communication.
## Signals are scaffolded for all 14 systems even if unused in early phases.
## This provides a stable contract for downstream development (Phases 2-9).
## Naming convention: past-tense snake_case (per D-03).

# -- Rider Lifecycle --
signal rider_spawned(rider: CharacterBody3D)
signal rider_damaged(rider: CharacterBody3D, amount: int, source: Node)
signal rider_knocked_out(rider: CharacterBody3D, launch_force: float, weapon_type: StringName)
signal rider_crash_landed(rider: CharacterBody3D)
signal rider_stood_up(rider: CharacterBody3D)
signal rider_started_walking(rider: CharacterBody3D)
signal rider_remounted(rider: CharacterBody3D)

# -- Combat --
signal attack_started(attacker: CharacterBody3D)
signal attack_hit(attacker: CharacterBody3D, target: CharacterBody3D, damage: int)

# -- Weapons --
signal weapon_picked_up(rider: CharacterBody3D, weapon_name: StringName)
signal weapon_broken(rider: CharacterBody3D)
signal weapon_swung(rider: CharacterBody3D, weapon_name: StringName)

# -- Nitro --
signal nitro_picked_up(rider: CharacterBody3D)
signal nitro_activated(rider: CharacterBody3D)
signal nitro_expired(rider: CharacterBody3D)

# -- Race --
signal race_countdown_tick(seconds_left: int)
signal race_started()
signal race_finished(rankings: Array)
signal race_restarted()
signal rider_finished(rider: CharacterBody3D, position: int)
signal race_position_changed(rider: CharacterBody3D, new_position: int)

# -- Camera / Juice --
signal slow_mo_requested(duration: float, time_scale: float)
signal screen_shake_requested(intensity: float, duration: float)
signal hit_flash_requested()

# -- System --
signal level_loaded(level_index: int)
signal game_paused()
signal game_resumed()
```

This is exactly 30 signals covering all 14 systems from systems-index.md.

**2. `game/core/main.gd`** -- Pipeline verification script:

```gdscript
extends Node3D
## Phase 1 pipeline verification scene.
## Validates: renderer, physics interpolation, input actions, signal bus.

func _ready() -> void:
    # Verify GameEvents autoload is accessible
    assert(GameEvents != null, "GameEvents autoload not found -- check project.godot [autoload] section")

    # Verify renderer is Compatibility
    var renderer := ProjectSettings.get_setting("rendering/renderer/rendering_method")
    assert(renderer == "gl_compatibility", "Renderer must be gl_compatibility, got: %s" % renderer)

    # Verify physics interpolation is enabled
    var interp := ProjectSettings.get_setting("physics/common/physics_interpolation")
    assert(interp == true, "Physics interpolation must be enabled")

    # Verify input actions exist
    var required_actions := ["move_forward", "move_back", "steer_left", "steer_right", "attack", "nitro", "restart", "pause"]
    for action in required_actions:
        assert(InputMap.has_action(action), "Missing input action: %s" % action)

    # Test signal bus round-trip
    var signal_received := false
    GameEvents.race_started.connect(func(): signal_received = true)
    GameEvents.race_started.emit()
    assert(signal_received, "Signal bus round-trip failed")

    print("[Phase 1] All pipeline checks passed:")
    print("  - Renderer: %s" % renderer)
    print("  - Physics interpolation: %s" % interp)
    print("  - Input actions: %d configured" % required_actions.size())
    print("  - Signal bus: round-trip OK")
    print("  - GameEvents signals: %d scaffolded" % GameEvents.get_signal_list().size())
```

**3. `game/core/main.tscn`** -- Minimal scene file:

```
[gd_scene load_steps=2 format=3 uid="uid://placeholder"]

[ext_resource type="Script" path="res://core/main.gd" id="1_main"]

[node name="Main" type="Node3D"]
script = ExtResource("1_main")
```

Note: The `uid` in the .tscn file should be auto-generated. If writing manually, use any valid uid format like `uid://bq1abc123`. Alternatively, create the .tscn without a uid line and let Godot assign one on first open.

Per D-04: the signal bus scaffolds ALL signals for all 14 systems. Per D-03: signals use past-tense snake_case naming.
  </action>
  <verify>
    <automated>grep -c "^signal " /Users/haocheng_mini/Documents/projects/my-game/game/core/game_events.gd | xargs -I{} test {} -ge 25 && grep -q "assert(GameEvents" /Users/haocheng_mini/Documents/projects/my-game/game/core/main.gd && test -f /Users/haocheng_mini/Documents/projects/my-game/game/core/main.tscn && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - game/core/game_events.gd contains at least 25 `signal` declarations (30 expected)
    - game/core/game_events.gd contains `signal rider_knocked_out` (rider lifecycle)
    - game/core/game_events.gd contains `signal weapon_picked_up` (weapons)
    - game/core/game_events.gd contains `signal race_started` (race)
    - game/core/game_events.gd contains `signal screen_shake_requested` (juice)
    - game/core/game_events.gd contains `signal level_loaded` (system)
    - game/core/game_events.gd first line is `extends Node`
    - game/core/main.gd contains `assert(GameEvents != null`
    - game/core/main.gd contains `InputMap.has_action`
    - game/core/main.tscn contains `type="Node3D"` and references `main.gd`
  </acceptance_criteria>
  <done>GameEvents signal bus has 30 typed signals covering all 14 game systems. Main scene runs pipeline verification assertions on startup.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep 'gl_compatibility' game/project.godot` returns a match (FOUND-01)
2. `grep 'physics_interpolation=true' game/project.godot` returns a match (FOUND-06)
3. `grep -c '^signal ' game/core/game_events.gd` returns >= 25 (FOUND-05)
4. `grep 'move_forward' game/project.godot` returns a match (FOUND-04)
5. All 8 input actions present: move_forward, move_back, steer_left, steer_right, attack, nitro, restart, pause
6. game/core/main.tscn exists and references main.gd
</verification>

<success_criteria>
- Godot project at game/ opens in editor showing "Compatibility" in title bar
- project.godot has Compatibility renderer, physics interpolation, SMAA, 8 input actions with dual bindings
- GameEvents autoload has 30 typed signals covering rider, combat, weapon, nitro, race, camera/juice, and system events
- Main scene runs all assertions without failure
- Directory structure follows D-01 pattern: game/core/, game/track/, game/tests/
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-pipeline/01-01-SUMMARY.md`
</output>
